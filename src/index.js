import React from "react";
import ReactDOM from "react-dom/client";
import "../src/asset/css/input.css";
import "../src/asset/css/style.css";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
